import delay from "@/utils/delay";

const Register = async () => {
  await delay(3000);

  return (
    <div className="page">
      <h1>Kayıt Olma Sayfası</h1>
    </div>
  );
};

export default Register;
