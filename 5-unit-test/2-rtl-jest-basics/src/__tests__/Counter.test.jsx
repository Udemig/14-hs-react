it("Sayaç doğru şekilde çalışır", () => {
  // 1) test edilecek bileşen render edilir
  // 2) gerekli elementleri al (buttons, p)
  // 3) sayacın değeri 0 mı kontrol et
  // 4) azlat butonu inaktif mi kontrol et
  // 5) arttır butonuna tıkla
  // 6) sayacın değeri 1 mi kontrol et
  // 7) azalt butonu aktif mi kontrol et
  // 5) arttır butonuna tıkla
  // 6) sayacın değeri 2 mi kontrol et
  // 8) azalt butonuna tıkla
  // 9) sayacın değeri 1 mı kontrol et
  // 8) azalt butonuna tıkla
  // 9) sayacın değeri 0 mı kontrol et
  // 10) azalt butonu inaktif mi kontrol et
});
